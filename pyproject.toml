[project]
name = "france-elec"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "arslane18", email = "ami54500@gmail.com" }
]
requires-python = ">=3.13.15,<3.14"
dependencies = [
    "fastapi>=0.141.1",
    "polars>=1.44.1",
    "python-dotenv>=1.2.3",
    "requests>=2.34.2",
]

[project.scripts]
france-elec = "france_elec:main"

[build-system]
requires = ["uv_build>=0.12.6,<0.13.0"]
build-backend = "uv_build"
